# Demo outputs

The main video examples are hosted on the separate portfolio website rather than committed directly to this repository.

**Portfolio:** `[ADD_PORTFOLIO_URL]`

Keeping large `.mp4` files outside the Git repository makes the project easier to clone and keeps the source-focused repository lightweight.

If public demo media is added here later, prefer compressed preview files and keep originals in external storage.
